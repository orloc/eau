{% extends 'AppBundle:Marketing:landing.html.twig' %}


{% block inner_content -%}
    {% trans_default_domain 'FOSUserBundle' %}

    <div class="container login-container">
        <div class="row">
            <div class="col-sm-6 col-sm-offset-3">
                <div class="login-panel panel panel-default">
                    <div class="panel-heading">
                        <h3 class="panel-title center-text">EAU<br><span class="small">Registration - Step Two</span></span>
                        </h3>
                    </div>
                    <div class="panel-body">
                        {% include ':common:flashes.html.twig' %}

                        <p class="text-center"><b class="text-success">Great!</b> You have authenticated with your character <b>{{ auth.name }}</b><br> <span class="text-info">This character will be automatically added to your account.</span></p>
                        <form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
                            <div class="form-group">
                                {{ form_errors(form.email) }}
                                {{ form_widget(form.email, { attr: { class: 'form-control', placeholder: 'Enter your email...'}}) }}
                            </div>

                            <div class="form-group">
                                {{ form_errors(form.username) }}
                                {{ form_widget(form.username, { attr: { class: 'form-control', placeholder: 'Pick a username', style: 'background-color: grey; color: black;'}}) }}
                            </div>

                            <div class="form-group">
                                {{ form_errors(form.plainPassword.first) }}
                                {{ form_widget(form.plainPassword.first, { attr: { class: 'form-control', placeholder: 'Password'}}) }}
                            </div>

                            <div class="form-group">
                                {{ form_errors(form.plainPassword.second) }}
                                {{ form_widget(form.plainPassword.second, { attr: { class: 'form-control', placeholder: 'Password confirmation'}}) }}
                            </div>
                            <div>
                                <input class="btn center-block" type="submit" value="{{ 'registration.submit'|trans }}" />
                            </div>
                            {{ form_widget(form) }}
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}


