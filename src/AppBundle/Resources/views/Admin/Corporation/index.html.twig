{% extends 'AppBundle:Admin:layout.html.twig' %}

{% block content -%}
    <div class="row" ng-controller="corpListController">
        <h3>Corporation <span class="muted small">LIST</span>
            <button type="button" ng-click="openNew()" class="pull-right btn btn-sm" ng-cloak>
                <i class="fa fa-plus" ng-show="!nav_open"></i>
                <i class="fa fa-minus" ng-show="nav_open"></i>
            </button>
        </h3>
        <div class="col-xs-12">
            <div class="row">
                <div class="col-xs-5">
                    <table class="table table-hover data-table table-condensed" ng-cloak>
                        <thead>
                        <th></th>
                        <th>Name</th>
                        <th>Created At</th>
                        </thead>
                        <tbody>
                        <tr ng-repeat="c in corps">
                            <td width="75px"><eve-image image-type="'Corporation'" object_id="c.eve_id" img-width="64"></eve-image> </td>
                            <td>{{ '{{ c.name }}' }}</td>
                            <td>{{ '{{ c.created_at | date:"shortDate" }}' }}<br>
                                <span class="small">{{ '{{ c.created_at | date:"shortTime" }}' }}</span>
                            </td>
                        </tr>
                        </tbody>
                    </table>

                </div>
                <div class="col-xs-7">
                </div>
            </div>
        </div>
        {% include 'AppBundle:Admin/Corporation:new.html.twig' %}
    </div>
{% endblock %}
